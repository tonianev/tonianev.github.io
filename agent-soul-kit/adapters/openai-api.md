# OpenAI API Adapter

Map the files into the `system` message.

## Pattern

- `system`: compiled soul + memory + posture
- `user`: live task

## Notes

- Keep task-specific instructions out of the base files when possible.
- If memory is not actually persisted, say so in the surrounding product.
- Prefer small durable files over one oversized system prompt.
